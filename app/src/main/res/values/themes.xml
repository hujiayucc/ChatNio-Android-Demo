<resources>
    <style name="Base.Theme.ChatNio" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/chatnio2</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="colorButtonNormal">@color/black</item>
    </style>

    <style name="Theme.ChatNio" parent="Base.Theme.ChatNio"/>
    <style name="Theme.ChatNio.Splash" parent="Base.Theme.ChatNio">
        <!-- 隐藏状态栏 -->
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>